Similar to react class in terms of lifecycle.
Lifecycle methods{
    before create=> Called after instance is initialised
    before mount => called before the render function
    mounted => Called after mount (After the page is loaded)
    updated => CAlled if there are any changes made to DOM
}

Created a sample application that handles user input and displays text.
Created a sample login with basic username == password check.
Explored on data and methods, syntax like v-if for conditional rendering, v-bind for dynamic property binding, v-on for DOM events.
